# ============================================
# Frontend 환경변수 (선택)
# ============================================
# 기본값은 src/api/client.ts에 fallback으로 들어있어
# 이 파일이 없어도 동작합니다.
#
# .env.local은 .gitignore에 의해 자동 제외됩니다.

# 백엔드 API 주소 (기본: http://localhost:6333/api/v1)
VITE_API_BASE_URL=http://localhost:6333/api/v1
